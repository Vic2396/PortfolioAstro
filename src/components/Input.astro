---
const { type, name } = Astro.props
---

<input type={ type } id={ name } name={ name } required 
        class="bg-gray-300 dark:bg-gray-600 rounded p-2 focus:outline-2
                focus:outline-slate-500 dark:focus:outline-slate-100 focus:ring-0">
