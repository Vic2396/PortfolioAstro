---
import Title from '../components/about/Title.astro'

const { title, className } = Astro.props
---

<section class={`h-full w-full px-4 py-6 flex flex-col gap-4 items-center ${className} font-text sm:rounded`}>
    <Title name={ title } />
    <slot />
</section>